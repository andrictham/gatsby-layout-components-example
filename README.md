# Gatsby Layout Components Example

This example uses a layout component to create a global site header with navigation links at `/src/layouts/index.js`.

Components are styled with `styled-components`.
